export enum MovieSortField {
    VIEWS = 'views',
    TITLE = 'title',
    CREATED = 'createdAt',
}

export enum MovieBlockType {
    IMAGE = 'IMAGE',
    TEXT = 'TEXT',
}

export enum MovieType {
    ALL = 'ALL',
    COMEDY = 'COMEDY',
    FANTASY = 'FANTASY',
    DRAMA = 'DRAMA',
}

export enum MovieView {
    BIG = 'BIG',
    SMALL = 'SMALL',
}
